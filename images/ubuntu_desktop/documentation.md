# UBUNTU Desktop

This image creates an UBUNTU desktop that is available directly from
a web interface. You can login with your username and password
which is set in the configuration.

## User

This service uses the default user *ubuntu*
